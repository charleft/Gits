Gits
====

Praticas con Git/Github

git init    :   Inicia repositorio
git status  :   Verifica estado del proyecto

git log --oneline --graph --all
git config --global alias.nicelog 'log --oneline --graph --all'


git reset --soft
git reset --mixed
git reset --hard

git checkout <commit | branch>
git merge <altBranch>